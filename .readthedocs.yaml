version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install uv
    - cd docs && uv sync --frozen
    - cd docs && uv run mkdocs build --strict --site-dir $READTHEDOCS_OUTPUT/html
